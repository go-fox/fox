version: v2

modules:
  - path: protos
    name: buf.build/fox/foxapis
    lint:
      use:
        - STANDARD
    breaking:
      use:
        - FILE
deps:
  - 'buf.build/googleapis/googleapis'
  - 'buf.build/gnostic/gnostic'
breaking:
  use:
    - FILE

lint:
  use:
    - DEFAULT
